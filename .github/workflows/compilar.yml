name: Compilar Firmware ESP32

on:
  push:
    branches: [ "main", "master", "principal" ]
  pull_request:
    branches: [ "main", "master", "principal" ]
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Baixar o codigo do repositorio
        uses: actions/checkout@v4

      - name: Configurar o Python Estavel
        uses: actions/setup-python@v5
        with:
          python-version: '3.10'

      - name: Instalar o PlatformIO
        run: |
          python -m pip install --upgrade pip
          pip install -U platformio

      - name: Preparar e Compilar o Projeto
        run: |
          # 1. Garante que a pasta destino existe
          mkdir -p nRFBox/src
          
          # 2. Busca e copia TODOS os arquivos de código de qualquer lugar para nRFBox/src/
          find . -type f \( -name "*.cpp" -o -name "*.h" -o -name "*.ino" \) -not -path "*/.pio/*" -exec cp {} nRFBox/src/ \; 2>/dev/null || true
          
          # 3. Força a criação do arquivo icone.h correto varrendo o conteúdo dos arquivos da pasta
          cd nRFBox/src
          find . -maxdepth 1 -type f -name "*.h" ! -name "icone.h" -exec grep -qE "unsigned char|static const" {} \; -exec cp {} icone.h \; 2>/dev/null || true
          cp *cone*.h icone.h 2>/dev/null || true
          
          # Garante que as chamadas no código fiquem padronizadas sem acento
          sed -i 's/"ícone.h"/"icone.h"/g' * 2>/dev/null || true
          cd ../..
          
          # 4. Garante que o arquivo platformio.ini principal vá para a pasta nRFBox
          if [ -f "platformio.ini" ]; then
            cp platformio.ini nRFBox/
          fi
          
          # 5. Instala todas as bibliotecas necessárias para o projeto (incluindo o CC1101 correto)
          cd nRFBox
          pio lib install "olikraus/U8g2"
          pio lib install "adafruit/Adafruit NeoPixel"
          pio lib install "nrf24/RF24"
          pio lib install "https://github.com/LSatan/SmartRC-CC1101-Driver-Lib.git"
          
          # 6. Roda a compilação final
          pio run

      - name: Guardar o arquivo compilado (.BIN)
        uses: actions/upload-artifact@v4
        with:
          name: nRFBox-Firmware
          path: nRFBox/.pio/build/**/*.bin
          
